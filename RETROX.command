#!/usr/bin/env bash
# Double-click ce fichier dans Finder pour lancer RETROX (macOS ouvre les
# .command dans Terminal automatiquement). Aucune installation requise.
cd "$(dirname "${BASH_SOURCE[0]}")"
exec ./run.sh
